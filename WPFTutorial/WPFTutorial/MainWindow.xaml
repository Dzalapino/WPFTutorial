<Window x:Class="WPFTutorial.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTutorial"
        mc:Ignorable="d"
        Title="WPF Tutorial" Height="450" Width="900">
    <StackPanel VerticalAlignment="Center" Margin="20">
        <Slider 
            x:Name="mySlider" Minimum="2" Maximum="100" Value="50"
            TickPlacement="BottomRight" TickFrequency="2" IsSnapToTickEnabled="True"
            ValueChanged="mySlider_ValueChanged"></Slider>
        <TextBlock 
            x:Name="myTextBlock" Height="250" TextWrapping="Wrap" 
            FontSize="{Binding ElementName=mySlider, Path=Value, UpdateSourceTrigger=PropertyChanged}">
            <Run Text="This is the font size: "/>
            <Run Text="{Binding ElementName=mySlider, Path=Value, UpdateSourceTrigger=PropertyChanged}"/>
        </TextBlock>
    </StackPanel>
</Window>
